void folder has a biome and palette, so ideally it goes with your biomes,
add_ship_void.yml goes with your extrusions,
and then just add that to the extrusions. In the default overworld pack it looks like this:

	extrusions:
		- << biome-providers/extrusions/add_ship_void.yml:extrusions



NOT thoroughly tested. Always make backups. Only tested on 1.20.1 for less than an hour.